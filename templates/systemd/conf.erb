# Config for Service <%= @prog %>
# Parameters to be passed to the server when started
SELENIUM_SERVER_ARGS="<%= @conf_options %>"

# Name of the X display to connect to when creating a new session
DISPLAY="<%= @display %>"